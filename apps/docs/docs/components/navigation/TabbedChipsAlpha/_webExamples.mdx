### Basic usage

```jsx live
function ExampleDefault() {
  const tabs = [
    { id: 'all', label: 'All' },
    { id: 'swap', label: 'Swap' },
    { id: 'collect', label: 'Collect' },
    { id: 'bridge', label: 'Bridge' },
  ];
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

```jsx live
function ExampleCompactNoStart() {
  const tabs = [
    { id: 'all', label: 'All', compact: true },
    { id: 'swap', label: 'Swap', compact: true },
    { id: 'collect', label: 'Collect', compact: true },
    { id: 'bridge', label: 'Bridge', compact: true },
  ];
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

### Many tabs (paddles)

:::tip Paddles & overflow
Paddles appear automatically when the tab list overflows. You can adjust their size via `paddleStyle`.
:::

```jsx live
function ExampleWithPaddles() {
  const tabs = Array.from({ length: 12 }).map((_, i) => ({
    id: `tab_${i + 1}`,
    label: `Tab ${i + 1}`,
  }));
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

### With custom sized paddles

```jsx live
function ExampleCustomPaddles() {
  const tabs = Array.from({ length: 10 }).map((_, i) => ({
    id: `t_${i + 1}`,
    label: `Item ${i + 1}`,
  }));
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return (
    <TabbedChipsAlpha
      activeTab={activeTab}
      onChange={setActiveTab}
      tabs={tabs}
      paddleStyle={{ transform: 'scale(0.8)' }}
    />
  );
}
```

### Long text tabs

```jsx live
function ExampleLongText() {
  const tabs = [
    { id: 'a', label: 'Very long tab label that can wrap on small widths' },
    { id: 'b', label: 'Another extra long label to test overflow' },
    { id: 'c', label: 'Short' },
  ];
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

### Disabled tab

```jsx live
function ExampleDisabled() {
  const base = [
    { id: 'first', label: 'First' },
    { id: 'second', label: 'Second' },
    { id: 'third', label: 'Third' },
  ];
  const tabs = base.map((t, i) => ({ ...t, disabled: i === 1 }));
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

### With start media

:::tip Media sizing
For start media, use circular images sized 24×24 for regular chips and 16×16 for compact chips.
:::

```jsx live
function ExampleWithStart() {
  const icon = { height: 24, width: 24, shape: 'circle', source: assets.eth.imageUrl };
  const tabs = [
    { id: 'all', label: 'All', start: <RemoteImage {...icon} /> },
    { id: 'swap', label: 'Swap', start: <RemoteImage {...icon} /> },
    { id: 'collect', label: 'Collect', start: <RemoteImage {...icon} /> },
    { id: 'bridge', label: 'Bridge', start: <RemoteImage {...icon} /> },
  ];
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

### Compact with start

```jsx live
function ExampleCompactWithStart() {
  const cicon = { height: 16, width: 16, shape: 'circle', source: assets.eth.imageUrl };
  const tabs = [
    { id: 'all', label: 'All', compact: true, start: <RemoteImage {...cicon} /> },
    { id: 'swap', label: 'Swap', compact: true, start: <RemoteImage {...cicon} /> },
    { id: 'collect', label: 'Collect', compact: true, start: <RemoteImage {...cicon} /> },
    { id: 'bridge', label: 'Bridge', compact: true, start: <RemoteImage {...cicon} /> },
  ];
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return <TabbedChipsAlpha activeTab={activeTab} onChange={setActiveTab} tabs={tabs} />;
}
```

### Custom TabComponent

:::tip Custom Tab behavior
When providing a custom TabComponent, use `useTabsContext()` and call `updateActiveTab(id)` to update selection state. Reflect the active state (e.g., `inverted` styling) based on `activeTab?.id === id`.
:::

```jsx live noInline
function CustomTab({ id, label, ...props }) {
  const { activeTab, updateActiveTab } = useTabsContext();
  const isActive = activeTab?.id === id;
  return (
    <Chip inverted={isActive} onClick={() => updateActiveTab(id)} width="max-content" {...props}>
      {label}
    </Chip>
  );
}

const tabs = [
  { id: 'all', label: 'All' },
  { id: 'swap', label: 'Swap' },
  { id: 'collect', label: 'Collect' },
];

function Example() {
  const [activeTab, setActiveTab] = useState(tabs[0]);
  return (
    <TabbedChipsAlpha
      activeTab={activeTab}
      onChange={setActiveTab}
      tabs={tabs}
      TabComponent={CustomTab}
    />
  );
}

render(<Example />);
```
