import { ComponentStylesTable } from '@site/src/components/page/ComponentStylesTable';
import { StylesExplorer } from '@site/src/components/page/StylesExplorer';
import { Pagination } from '@coinbase/cds-web/pagination';

import webStylesData from ':docgen/web/pagination/Pagination/styles-data';

## Selectors

<ComponentStylesTable componentName="Pagination" styles={webStylesData} />

## Explorer

<StylesExplorer selectors={webStylesData.selectors}>
  {(classNames) => (
    <Pagination
      totalPages={10}
      activePage={5}
      onChange={() => {}}
      showFirstLastButtons
      classNames={classNames}
    />
  )}
</StylesExplorer>
